# Override with own variables
apt:  # noqa: var-naming[no-role-prefix] - see doc/STATUS.adoc
  packages:
    # Python3 should always be installed
    # on managed hosts when using ansible
    - python3
    - openssh-server
