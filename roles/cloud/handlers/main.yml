- name: Reload nginx
  ansible.builtin.systemd_service:
    name: nginx
    state: reloaded
    enabled: true
- name: Reload PHP-FPM
  ansible.builtin.systemd_service:
    name: "php{{ php_version }}-fpm"
    state: reloaded
    enabled: true
