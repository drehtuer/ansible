# Override with own variables
users:
  # Default user root
  - name: "root"
    group: "root"
    shell: "/usr/bin/sh"
    groups: ""
    create_home: true
    password: "*"

# Needs at least one entry for
# ansible `loop` function
authorized_keys:
# - name: <name>
# - public_key: <public key as string>
